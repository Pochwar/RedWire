<!-- Navigation -->
<nav class="navbar white navbar-default ct-no-margin navbar-static-top">
    
    <div class="container-fluid">
        <div class="row row-eq-height">

            <div class="col-md-3">
                <!--navbar brand-->
                <div class="navbar-brand ct-navbar-brand" href="#page-top">
                    <img src="img/logo.png">
                    <span>
                        <p class="ct-txt-yellow ct-txt-larger">{{ __("SITENAME") }}</p>
                        <p class="ct-txt-large">{{ __("SITESUBNAME") }}</p>
                    </span>
                </div>
            </div>

            <div class="col-md-6 col-flex-centered">
                {% include "./searchbar.twig" %}
            </div>

            <!--bouton toggle -->
            <div class="col-md-3 col-flex-centered" >
                <div class="navbar-header page-scroll">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#menu-responsive">
                        <span class="sr-only">Menu</span> Menu <i class="fa fa-bars"></i>
                    </button>
                </div>
            
                <!-- Menu responsive -->
                <div class="collapse navbar-collapse" id="menu-responsive">
                    <ul class="nav navbar-nav navbar-right">
                        <li>
                            <a href="#modal_connection" data-toggle="modal" class="btn btn-default ct-mr-1">{{__("SIGNIN") }}</a>
                        </li>
                        <li>
                            <a href="register" class="btn btn-default ct-mr-1">{{__("SIGNUP") }}</a>
                        </li>
                        <li>
                            <a href="lang/fr" class="btn btn-primary btn-xs ct-nav-lang">fr</a>
                        </li>
                        <li>
                            <a href="lang/en" class="btn btn-primary btn-xs ct-nav-lang">en</a>
                        </li>
                    </ul>
                </div>
                </div>
            </div>
        </div>
    </div>
</nav>

<!-- modal de connection -->
<div class="modal fade" id="modal_connection" tabindex="-1" role="dialog" aria-hidden="true">   
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <form id="login_form">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title">{{ __("LOGIN") }}</h4>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                        <label for="connection_email">{{ __("YOUREMAIL") }}</label>
                        <input type="email" class="form-control" id="connection_email" placeholder="Email" />
                    </div>
                    <div class="form-group">
                        <label for="connection_pwd">{{ __("YOURPASS") }}</label>
                        <input type="password" class="form-control" id="connection_pwd" placeholder="mot de passe" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">{{ __("CANCEL") }}</button>
                    <input type="submit" class="btn ct-bg-yellow" value="{{ __("SIGNIN") }}" />
                </div>
            </form>
        </div>
    </div>
</div>

