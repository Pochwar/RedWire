<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
    <meta charset="UTF-8">
    <title>{{ __("SITENAME") }} - {{ __("HOME") }}</title>
</head>
<body>
<p>{{ __("HELLO") }}</p>
<p>{{ __("AUTH") }} {{ pseudo }}</p>
</body>
</html>